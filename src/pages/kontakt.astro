---
import Base from "../layouts/Base.astro";
import ContactPage from "../components/ContactPage.astro";
import { t } from "../i18n";
const locale = "sv" as const;
---
<Base locale={locale} pageKey="contact" title={`${t(locale, "nav.contact")} — Salong NOVO`} description="Rörstrandsgatan 39C, Stockholm. T-bana S:t Eriksplan. 08-663 30 14.">
  <ContactPage locale={locale} />
</Base>
