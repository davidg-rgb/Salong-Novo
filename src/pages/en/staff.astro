---
import Base from "../../layouts/Base.astro";
import StaffPage from "../../components/StaffPage.astro";
import { t } from "../../i18n";
const locale = "en" as const;
---
<Base locale={locale} pageKey="staff" title={`${t(locale, "nav.staff")} — Salong NOVO`} description="Meet NOVO's 18 award-winning stylists in Vasastan, Stockholm.">
  <StaffPage locale={locale} />
</Base>
