---
import Base from "../../layouts/Base.astro";
import ContactPage from "../../components/ContactPage.astro";
import { t } from "../../i18n";
const locale = "en" as const;
---
<Base locale={locale} pageKey="contact" title={`${t(locale, "nav.contact")} — Salong NOVO`} description="Rörstrandsgatan 39C, Stockholm. Metro S:t Eriksplan. +46 8 663 30 14.">
  <ContactPage locale={locale} />
</Base>
